# Dev B — Бот + Веб-дашборд (8 часов)

## Файлы задачи
- `src/bot/main.py` — бот
- `src/web/index.html` — главный дашборд
- `src/web/analysis.html` — страница анализа запроса

Dev A не трогает эти файлы → конфликтов нет.

---

## БЛОК 1 — Telegram Bot (`src/bot/main.py`)

### Задачи

- [ ] **1. Убрать ReplyKeyboardMarkup**
  Удалить переменную `QUICK_REPLIES` и все `reply_markup=QUICK_REPLIES` в функциях `start`, `help_cmd`, `handle_message`.
  Verify: бот отвечает без клавиатуры снизу

- [ ] **2. Добавить typing-индикатор**
  Добавить в начало `handle_message` (до запроса в API):
  ```python
  from telegram.constants import ChatAction
  await context.bot.send_chat_action(chat_id=update.effective_chat.id, action=ChatAction.TYPING)
  ```
  Verify: в Telegram видно "печатает..." пока бот думает

- [ ] **3. Переписать `/start`**
  Тон — живой оператор поддержки, без упоминания кнопок:
  ```
  Здравствуйте! Я AI-помощник сервис-деска «Балтийский Берег».
  Опишите вашу проблему — найду решение в базе знаний.
  ```
  Verify: `/start` выдаёт новый текст без упоминания кнопок

- [ ] **4. Добавить команду `/new`**
  Просто отвечает: "Хорошо, начнём сначала. Опишите вашу проблему."
  Зарегистрировать handler: `CommandHandler("new", new_cmd)`
  Verify: `/new` работает

- [ ] **5. Переделать формат ответа пользователю**
  Убрать из `handle_message`: score-бар (`█░`), `localhost:8001/analysis/...`, classification-метрики.
  Оставить только:
  - текст ответа
  - если `top_source` есть → `📎 <Тикет|KB>: <title>`
  - если `escalated` → отдельная строка с фразой эскалации

  Итоговый вид:
  ```
  Проблема с VPN решается так:
  1. Проверьте клиент UniVPN (иконка в трее)
  2. Переподключитесь — правой кнопкой → "Подключить"

  📎 Тикет: Настройка удалённого доступа L2TP
  ```
  Verify: нет "░█", нет `localhost`, нет "Сеть и VPN · 🟡 Средний"

- [ ] **6. Улучшить текст эскалации**
  Заменить "Рекомендую создать заявку в сервис-деске, если ответ не помог." на:
  ```
  ⚠️ Не нашёл точного решения. Рекомендую обратиться к специалисту поддержки или создать заявку.
  ```
  Verify: фраза появляется при `escalated=True`

---

## БЛОК 2 — Главный дашборд (`src/web/index.html`)

### Что сейчас не так
- Статистика только по текущей сессии (in-memory, сбрасывается при рестарте)
- Dev A добавил `/db-stats` — реальные данные из MSSQL за 12 месяцев
- Графики "По категориям" (doughnut) и "По приоритетам" (bar) — из сессии, мало данных, выглядят пусто

### Задачи

- [ ] **7. Добавить блок "Статистика базы" из `/db-stats`**
  Разместить ДО блока статистики сессии. Загружать при `init()`:
  ```js
  async loadDbStats() {
    try {
      const r = await fetch('/db-stats');
      this.dbStats = await r.json();
    } catch {}
  }
  ```
  Показать 4 карточки-счётчика:
  - Всего заявок за год
  - Закрыто
  - Просрочено
  - Среднее время решения (часы)

  Verify: блок появляется при загрузке, данные не нулевые

- [ ] **8. Заменить doughnut "По категориям" на горизонтальный бар из `/db-stats`**
  Источник: `dbStats.top_services` (топ-10 сервисов из реальной БД).
  Тип графика: `'bar'` с `indexAxis: 'y'` (горизонтальный).
  Убрать doughnut — он плохо читается на малом количестве точек.
  Verify: видны реальные сервисы с реальными цифрами

- [ ] **9. Заменить "Динамика сессии" (historyChart) на линейный график из `/db-stats`**
  Источник: `dbStats.monthly` — динамика по месяцам из реальной БД.
  Текущий `historyChart` (время ответа в мс по запросам сессии) — убрать, пользы мало.
  Новый: количество заявок по месяцам за 12 месяцев.
  Verify: линейный график с 12 точками (месяцами)

- [ ] **10. Оставить "По приоритетам" как есть**
  Данные из сессии. Бар-чарт нормальный, не трогать.

---

## БЛОК 3 — Страница анализа (`src/web/analysis.html`)

### Что сейчас не так
- График "Уверенность по источникам" (bar) — дублирует информацию из списка чанков ниже
- Блок "Распределение времени" (stacked bar) — полезен для разработчика, не для демо жюри

### Задачи

- [ ] **11. Убрать график "Уверенность по источникам"** (`scoreChart` canvas)
  Информация уже есть в списке чанков со score-баром → дублирование.
  Удалить: `<canvas id="scoreChart">`, функцию `renderChart()`, вызов в `init()`.
  Verify: страница грузится без графика, чанки остаются

- [ ] **12. Обновить бейдж источника в чанках**
  Dev A изменил `format_context()` — теперь источники: `kb`, `ticket`, `expense`.
  Добавить в шаблон чанка третий вариант:
  ```html
  :class="chunk.source === 'kb' ? 'bg-purple-100 text-purple-700' :
          chunk.source === 'expense' ? 'bg-amber-100 text-amber-700' :
          'bg-cyan-100 text-cyan-700'"
  x-text="chunk.source === 'kb' ? 'KB-статья' :
          chunk.source === 'expense' ? 'Решение' : 'Тикет'"
  ```
  То же сделать в легенде (`.flex.gap-3`): добавить третью точку — amber, "Решение".
  Verify: чанки из expenses отображаются с меткой "Решение" (янтарный цвет)

- [ ] **13. Блок "Распределение времени" — оставить**
  Он полезен на демо: показывает что 95% времени — это LLM, а поиск быстрый.
  Не трогать.

---

## Done When
- [ ] Бот без кнопок, с typing, чистым ответом
- [ ] Дашборд показывает реальную статистику из БД (не только сессию)
- [ ] На странице анализа нет дублирующего графика, метки источников правильные
